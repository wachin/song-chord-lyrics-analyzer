"""CLI command implementations.

Each module exposes ``add_arguments(parser)`` and ``run(args) -> int`` so the
top-level parser stays a thin composition root.
"""

from __future__ import annotations

__all__: list[str] = []
